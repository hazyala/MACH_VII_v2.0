name: MACH_VII_v2.0
channels:
  - conda-forge
  - defaults
dependencies:
  - python=3.10
  - pip
  # --- [콘다 직접 관리 부품: 충돌 방지용] ---
  - numpy=1.26.4
  - packaging>=24.0   
  - scipy
  - pybullet
  - pip:
      # --- [조종석 및 0.2.x 지능] ---
      - streamlit==1.30.0
      - opencv-python==4.8.0.76
      - ultralytics>=8.3.0
      - pyrealsense2>=2.55.0
      - langchain==0.2.14
      - langchain-community==0.2.12
      - langchain-core==0.2.43
      - langsmith<0.2.0
      - requests
      - Pillow
      - python-dotenv
      - fastapi
      - uvicorn
      - python-socketio
      - websocket-client